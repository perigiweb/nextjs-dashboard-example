import { Inter, Jost } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter'
})

export const jost = Jost({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-jost',
})